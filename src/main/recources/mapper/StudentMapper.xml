<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--命名空：namespace的名称必须指向刚刚接口类 -->
<mapper namespace="com.jc.mapper.StudentMapper">
    <resultMap id="DemoResultMap" type="Student">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Mon Dec 04 11:17:43 CST 2017.
        -->
        <id column="sid" property="sid" jdbcType="INTEGER" />
        <result column="sname" property="sname" jdbcType="VARCHAR" />
        <result column="sex" property="sex" jdbcType="VARCHAR" />
        <result column="clazz" property="clazz" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>

    </resultMap>
    <!--查询全部-->
    <select id="findAll" resultMap="DemoResultMap">
        SELECT * FROM student limit #{start,jdbcType=DECIMAL},#{end,jdbcType=DECIMAL}
    </select>

    <insert id="add" parameterType="Student">
        INSERT INTO student(sname,sex,clazz,password)
        VALUES (#{sname},#{sex},#{clazz},#{password})
    </insert>

   <delete id="delete" parameterType="int">
       DELETE FROM student WHERE sid=#{0}
   </delete>

    <update id="update" parameterType="student">
        UPDATE student SET sname=#{sname},sex=#{sex},clazz=#{clazz},password=#{password} WHERE sid=#{sid}
    </update>

    <select id="findById" parameterType="int" resultType="student">
        SELECT * FROM  student WHERE sid=#{0}
    </select>

    <select id="cout" parameterType="map" resultType="int" >
        SELECT count(1) FROM  student
    </select>
</mapper>
